#
# Example environment overrides for the IP400 services.
# Copy this file to /etc/ip400/ip400.env (the install script will do this
# automatically if it does not already exist) and adjust the values to match
# your deployment.
#

# Base paths
IP400_HOME=/home/ip400
IP400_SERVER_VENV=/home/ip400/ip400_server/.venv

# SPI bridge options
IP400_SPI_DEVICE=/dev/spidev0.0
IP400_SPI_HOST=127.0.0.1
IP400_SPI_PORT=9000
IP400_SPI_LISTEN=9001
IP400_SPI_DEBUG=1

# Flask server options
IP400_SERVER_HOST=0.0.0.0
IP400_SERVER_PORT=5000
